aws:
  region: "us-east-1"
  endpoint: "http://localhost:4566"
  credentials:
    type: "static"
    id: "test"
    secret: "test"

certificates:
  sources:
    acm:
      type: acm
      cache-path: "testdata/.tmp/certs"
