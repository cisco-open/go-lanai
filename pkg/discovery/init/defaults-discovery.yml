cloud:
  consul:
    discovery:
      port: ${server.port}
      scheme: http
      health-check-path: "${server.context-path}/admin/health"
      health-check-interval: 15s
      health-check-critical-timeout: 60s #deregisters an unhealthy instance after 60s
